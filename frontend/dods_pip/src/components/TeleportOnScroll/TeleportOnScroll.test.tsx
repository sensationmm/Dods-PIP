import TeleportOnScroll from './';

describe('TeleportOnScroll', () => {
  describe('when child element is in view', () => {
    it('should not render teleported component', () => {});
  });

  describe('when child element is out of view', () => {
    it('should render teleported component', () => {});

    describe('and teleport element is set to sticky', () => {
      it('should render the teleport element with sticky styles', () => {});
    });
  });
});
